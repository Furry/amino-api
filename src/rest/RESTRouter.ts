import * as Constants from "../utils/Constants"
import RESTManager from "./RESTManager"

export function buildRoute(rest: RESTManager, params = []) {
    const route = []

    return {
        
    }
}